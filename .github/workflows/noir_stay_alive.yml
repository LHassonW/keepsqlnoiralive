name: Keep Noir Website Alive

on:
  schedule:
    # Runs every day at 5:00 AM UTC
    - cron: '0 5 * * *'
  workflow_dispatch: # Allows you to test it manually

jobs:
  ping_site:
    runs-on: ubuntu-latest
    steps:
      - name: Visit Noir Website
        run: |
          curl -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/121.0.0.0 Safari/537.36" -L https://noir.great-site.net/
          echo "✅ Visit successful. InfinityFree logs updated."
